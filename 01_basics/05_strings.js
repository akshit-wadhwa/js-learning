const name = "Akshit"
const repoCount = 7

console.log(`Hello my name is ${name} and my repo count is ${repoCount}`); // Prefered way of fetching string values.
const mail = new String("Akshit-gmail-com")

console.log(mail[0])
console.log(mail.slice(0,6))
console.log(mail.split("-"))
console.log(mail.length)
console.log(mail.__proto__);
